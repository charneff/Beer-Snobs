<div id="beer-info">

    <h1><%= @beer.name %>  </h1>

    <h2>by <%= @beer.brewery.name %> </h2>

      <p><%= @beer.flavor_profile %></p>
      <%= link_to "See Reviews", beer_reviews_path(@beer.id) %>


    <%= form_tag beer_path(@beer.id), method: "delete" do %>
      <%= submit_tag "Delete #{@beer.name}" %>
    <% end %>
</div>
